// ------------------------------
// getElementsByTagName Example
// ------------------------------

// Selects all <button> elements in the document
let element = document.getElementsByTagName('button');

// Logs the HTMLCollection (array-like object) to console
console.log(element);
